{
  "name": "user-basic-mongo-storage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "mocha --reporter spec --recursive"
  },
  "dependencies": {
    "bluebird": "^3.4.1",
    "mongoose": "^4.5.5"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3"
  }
}
